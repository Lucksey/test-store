import React from "react";
import s from './Header.module.css';

const Header = () => {
    return <div className={s.wrapper}>
        <div>LOGO, Search, auth(sign-in)</div>
        </div>

}
export default Header;